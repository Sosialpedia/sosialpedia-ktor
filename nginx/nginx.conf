server {
    listen 80;
    listen [::]:80;

    server_name sosialpedia.xyz www.sosialpedia.xyz;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://sosialpedia.xyz$request_uri;
    }
}